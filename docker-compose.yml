version: "3"
services:
  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
  gotodo:
    build: .
    ports:
      - "3000:3000"
    links:
      - redis
    environment:
      - REDIS_HOST=redis
